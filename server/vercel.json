{
   "version": 2,
   "builds": [
     {
       "src": "server/index.js",
       "use": "@vercel/node",
       "config": {
         "installCommand": "npm install",
         "scripts": {
           "build": "npm install vite && vite build"
         }
       }
     }
   ]
 }